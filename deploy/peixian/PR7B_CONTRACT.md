# PR-7B 容量等待实施契约

基线为 PR7A 本机验收提交 0c79ecbea，分支 codex/pr7b-capacity-wait。仅合成独立环境；原服务、账号和 PR6 发布阻断保持不变。自动休眠关闭。

使用既有 v5 字段，不修改已经执行的 v4/v5 迁移脚本。新增 v5 当前策略验证器；持久策略版本 2 才能开启等待，旧 7A 代码拒绝该策略。配置变化不能静默切换已有库；需要冻结、无有效任务和显式离线策略变更许可。新增 runtime_pool_wait_v1 能力，旧 Worker 在副作用前拒绝。

有效启动先登记 waiting_capacity，原 enqueue_seq FIFO、TTL 1800 秒、上限 1000、每账号一条；重试不续期。等待无租约/attempt/卷/名额。原子分配同时更新 Job 和 Runtime；取消、过期、身份停用与人工禁止不会晋升。统一分配器由新申请、已确认终态及 Control 有界 tick 调用。维护和容量未知不晋升；安全修复保持旧 Slot，不先释放再抢回。配置冻结仍在 claim。

控制进程 tick 每 10 秒，单批 8；执行器长操作不阻塞等待晋升。停止已预留申请仍通过原排空和核对。等待结果只提示近似位置、到期与取消，不承诺启动时间，不自动提交旧问题。

测试覆盖最后名额竞争、重试/重启顺序、满队列、TTL、取消、维护/unknown、旧能力拒绝、备份恢复不自动排队。真实本机复验 A/B 占用，C 等待，A 停止后 C 获得自己的环境且 B 不受影响。通过情况单独记录，不作为生产或高并发证明。
