# 两个场景 Skill 与资料页面交付记录

日期：2026-09-18。服务器 frontend-alignment 工作区；源基线 630a0929d037812d43e418ae5539471d5e5c755e。

## 结论

实现、插件发布、两账号实际工具链与页面联动已完成。完整验收为部分通过：4 次模型请求均正确加载 Skill、调用所需工具、产生来源卡片；人工复核在 3 次文字摘要中发现对象归属、快照或观测依据错误。没有将这些文字作为已验证卡片。不能宣布文字准确性通过，也不能用于真实案件判断。

## 实现

- 插件 peixian-synthetic-records@1.1.0 保留原七个工具和 50 条资料；新增只接受两个固定场景 ID 的本地只读上下文工具。
- 两个独立 Skill 1.0.0 已发布为超级管理员模板，并复制至 alignment-a / alignment-b。原 Skill 未覆盖。新副本作为可用演示能力保留。
- 证据 API 按账号与会话鉴权，只投影当前请求已完成的、获授权且应用版本正确的插件结果。固定场景资料和记录逐项校验，不解析模型文字生成事实。
- 资料摘要、实际步骤、按时间排列的来源卡片、详情抽屉、窄屏入口、会话切换清理与未核验文字提示已上线。
- 修复恢复流程中旧失败责任阻塞恢复的问题：仅已确认暂停且停止、没有开放执行尝试时，清理早于成功暂停的失败恢复责任；未知状态和更新责任不清理。

## 验证

| 层次 | 结果 |
|---|---|
| Control 定向回归 | 56 passed；含证据、归属、版本、恢复、角色及 OpenAPI |
| 合成服务与验收规则 | 34 passed，13 subtests passed |
| 原插件与新场景插件 | 5 passed，50 assertions |
| 前端原有回归 | 57 passed；类型检查与构建通过 |
| 真实模型 | 两账号各两个场景，共 4 请求，无失败自动重试；工具链均完成 |
| 证据卡片 | 涉赌每账号 16 条，盗窃每账号 13 条；跨账号读取 4 次均 404 |
| 浏览器真实接口 | 1366/1920/390；抽屉 Escape、焦点恢复、无横向溢出、会话清理、空状态、中文草稿均通过，页面异常 0 |
| 人工文字检查 | 3 次需修正；1 次本次未发现实质问题，不能外推为模型总是正确 |

此次没有额外执行断网故障、进行中停止生成、完整前端恶意内容注入或整个平台全量回归。服务端测试验证了模型 HTML 不成为事实卡片；不能替代全部浏览器注入检查。没有大规模并发或真实资料测试。

## 运行版本与恢复

最终 Control 镜像 agent-platform-control:scenario-final-20260918，镜像 ID sha256:bb6d0021d7cf499ab191399e656db0c9100ce88526ceacfb5e8c02f7ea262a82。Agent/Gateway 沿用 alignment-630a0929d。
构建复用了已验证依赖镜像，无 Docker Hub 下载。Scenario.Control.Dockerfile 中的基础镜像必须在目标主机可用。

更新首次重建 Control 后，动态管理网络未自动挂回，导致环境观测失败。现已恢复两个账号的管理网络，并在当前 Compose 声明为 external，使重建保留连接。两账号最终均 ready。其他部署服务未升级。通用 Compose 再生成仍须核对管理网连接，此部署修正不等于全平台网络渲染已改造。

升级前已保存控制卷备份、部署配置、源文件及匹配私密文件，仅保留服务器私密目录，未纳入 Git。此次控制卷备份不是用户持久卷全量灾备。保留插件 1.0.0 和旧镜像回退路径；不静默覆盖用户新数据。

## 使用及后续

从统一 HTTPS 入口登录，选择上述 Skill 后整理固定 DEMO 场景。来源卡片可展开记录、快照和局限。模型摘要已经显示未逐项核验提示；复核对象归属以卡片与原始记录为准。

后续应针对 group_ref/member_ref/co_member_ref 映射、两套快照、资料覆盖范围与独行来源强化摘要约束，再单独申请新一轮小规模模型验证。本轮 4 次预算已用完，不自动增加请求。

本轮只保存服务器工作区的本地 Git 提交，不推送远端；密钥、账户密码、运行数据及私人报告不进入仓库。
