# 合成资料验收规则适配记录

日期：2026-09-18。执行位置：服务器 frontend-alignment 工作区。
基线提交：630a0929d037812d43e418ae5539471d5e5c755e；本次修改尚未提交。

## 完成项

- 显式选择 legacy / enhanced 夹具，报告记录固定快照、规则版本和源码摘要。
- enhanced 七模块记录数为 8、8、6、6、8、6、8，共 50 条；legacy 保留 30 条回退规则。
- 完整当前轮次工具链要求 Skill → 唯一对应资料工具；工具参数必须为空，调用必须完成。
- 严格比较工具标量、类型与完整 items；回答必须包含全部本模块 record_id，拒绝未知 DEMO 编号，允许结果中实际返回的关联引用。
- 修复 source_type 测试为既有模块枚举，并使夹具测试可被 pytest 自动发现。
- 更新三个验收驱动的必填版本参数及离线发布显式白名单，保留原历史验收报告。

## 本次实际验证

- Python：42 passed，15 subtests passed（服务契约、插件包、完整链证据、双版本与发布包测试）。
- Bun：3 passed，26 expectations（固定七工具、失败关闭和健康检查）。
- 修改的 Python 文件编译、三个验收入口 --help 通过。
- git diff --check 通过；发布白名单指向的合成资料文件全部存在。

## 验收边界

本轮未发起模型请求，未进行服务端黑盒复测，未重启运行服务，未修改账号授权或个人 Skill。
新增规则的增强快照真实模型行为验收仍未执行。旧快照七请求通过记录保持原样，不能作为新快照验收证据。
现有其他工作区修改保持原状，没有执行 Git 提交或推送。用户原 D 盘源文件未覆盖；本交付包提供服务器适配后的源码。

执行新版验收的命令和流程见源码目录 README.md。本次只检查命令参数，不执行该命令。
