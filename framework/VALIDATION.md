# 框架 0.1 验证记录

本分支从沛县检查点 2f87198cc963b8f4253b94353c7306d5a2485c69 派生，保持 SolidJS + FastAPI。框架源码工作区为 codex/agent-framework；旧部署源码与凭据不迁入本框架的本地运行状态。

## 已执行

| 验证集合 | 结果 | 范围 |
| --- | --- | --- |
| 控制层完整回归 | 142 项通过 | 原行为、Cookie 命名、命名空间、禁用旧迁移、账号资源边界 |
| 宿主运行层 | 66 项通过 | 原 48 项兼容用例、18 项部署命名空间/状态/锁/资源归属用例；未操作 Docker |
| 框架脚手架 | 26 项通过 | 真实临时 Git 导出、私密排除、文件与目录链接、覆盖拒绝、配置、凭据保持、部署参数和归属 |
| 前端 | typecheck、build、Prettier 通过 | 新工作区独立依赖，未修改旧工作区依赖 |
| 品牌页面 | 独立浏览器检查通过 | 产品名、副标题、页面标题、主题偏好键与 390px 布局；所有 API 被测试拦截，未登录 |

最终品牌构建为 index-DmuwJMeh.js，SHA-256 为 790a06c14d5b1e1cd4c30f02d4b798e6f2d791f2071ebee6999045d191368fe8；CSS 与原版一致。浏览器证据位于 packages/peixian-console/output/playwright/framework-brand，产物不进入 Git。

控制层测试依赖提示 FastAPI/Starlette 上游弃用告警；未据此跳过失败。Vite 构建有根工作区未安装 @tsconfig/bun 的非阻塞提示，本包类型检查与构建均为退出码 0。

## 真实模板生成与首次初始化

从框架提交 a2878d48c63cf7bce9542e01d53b0c66e9da35bc 实际生成独立示例项目，导出 6,526 个文件，包含安全物化后的模板链接。随后在 Windows 完成 init 与重复 init：

- 示例使用新 Git 仓库，没有继承提交历史或远程。
- LICENSE 与模板原字节相同；记录模板来源提交。
- 旧 A/B 迁移脚本和旧部署私密目录未被导出。
- 生成三份全新凭据，均与原沛县同类凭据不同；重复初始化后文件摘要不变。
- 新项目控制入口为 14200，namespace 为 starter-demo，Cookie 名为 starter_demo_session。
- 私密目录和状态文件被 Git 忽略；没有启动 Docker 容器或调用模型。
- 脱敏机器记录位于 framework/.runtime/generation-verification.json，未纳入提交。

原沛县 worktree 仍保持检查点源码且工作区干净，14090 的健康接口返回 ok。新框架构建和模拟测试没有替换该服务。

## 验证边界

- 沛县检查点曾通过真实 Docker 和 DeepSeek 验收；这些证据不能自动作为框架修改后端到端验收。
- 本轮没有重建或替换沛县运行镜像，没有启动框架 Agent、迁移旧 A/B、连接真实模型或业务数据接口。
- Cookie/命名空间测试使用独立测试应用和合成状态；宿主运行层使用模拟 Docker 响应和实际 OS 文件锁，未声明真实多项目容器部署通过。
- 一台 Docker Engine 只支持一个活跃项目的账号环境；不声明多用户、多操作系统用户或集群全局预算锁。
- 框架 0.1 提供干净项目生成与部署定义；业务模块注册、完整 RBAC、数据库迁移和 CRUD 生成器见 ROADMAP。
