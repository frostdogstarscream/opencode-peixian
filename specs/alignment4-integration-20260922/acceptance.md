# 第四轮前后端联调验收报告

日期：2026-09-22。范围：前端样式保留、个人空间恢复、线索详情、消息附件、实体图谱定向联调。资料全部为现有合成测试数据，不构成真实资料源或公安业务判断验收。

## 1. 完成状态

匹配前后端已发布至 `https://36.134.45.38:19460/`。

- 源码提交：`40cb937a4cdc891aac1fabb10ada31c251ba1d49`，分支 `codex/frontend-contracts`，服务器本地提交，未推送 GitHub。
- 镜像：`agent-platform-control:alignment4-40cb937a4cdc`。
- 镜像摘要：`sha256:d5318fc4e201e83318997a5da0c5306e91f0c56603f267c5d6a0f80647d51aee`。
- 运行镜像中 86 个 Control/shared Python 文件与提交源码匹配。
- 5 个前端 CSS 源文件与前端同事当前工作区逐文件一致；运行 CSS 产物仍为 `index-DI4D4ule.css`，SHA256=`6939df9f518d754748bf99bb3b524f66b3aa47cd4d957630c4d8ed3da5eadd1b`，与发布前一致。
- A、B 均 `ready/open`，维护模式 `normal`，恢复责任0、安全待处理0、容量健康；原配置版本54/27保持。
- 现有数据、会话和同事前端工作区保留；没有修改数据库结构、替换样式或推送远端。

## 2. 实现与真实联调

| 项目 | 结果 | 证据与边界 |
|---|---|---|
| Control 重建后的账号恢复 | 通过 | 先恢复管理网，再注册真实启动身份并核对冻结快照；修复模式退出后补排新观测。最终两账号自动回到 ready/open，没有直接改数据库状态 |
| 功能开关持久化 | 通过 | 三项 UID 白名单进入统一配置，正式管理脚本 up 成功，支持schema9镜像 |
| 附件与消息绑定 | 通过 | 本人消息返回提交时冻结的文件名；真实浏览器刷新后仍显示；本轮测试文件删除后名称保留、状态unavailable、下载404 |
| 线索详情 | 通过 | 最终真实资金请求取得12条记录，涉及7个自然日，收入5条、支出7条、未知0条；新增摘要和4条要点，12条来源可追溯 |
| 图谱实际接口和画布 | 定向通过 | B的资金Run产生ready来源图；断网恢复后同一canvas保留；缩放拖拽、路径、高亮和放大窗口已操作。未做持续忙碌压力或长时间重连高峰 |
| 权限 | 通过 | 跨账号消息/Run/文件/证据404；普通管理员和普通用户调用恢复接口403 |
| 桌面与抽屉 | 定向通过 | 1366详情抽屉可读，1920/1366截图已保存，CSS产物完全不变 |
| 390窄屏 | 未通过可读性检查 | document宽度没有溢出，但原背景导航占位挤压内容。为遵守不改样式，作为明确遗留，不把无溢出当作可用性通过 |

## 3. 请求与预算

本轮登记3次用户级请求；沿用账本累计8/40，剩余32。范围提示、失败或未知均计数，不自动重试。

1. A复杂资金+附件请求：HTTP202，Run正常结束，但范围校验给出不支持的对象/组合提示，未调用模型取数。仅用于附件归属和刷新验证，不记作资料取数成功。
2. B独立请求“整理资金往来”：实际工具链完成，形成12条记录、新摘要和ready关系图；同时暴露原显示层漏识别credit/debit，随后修正。
3. 最终镜像中A独立请求“整理资金往来”并带测试附件：实际工具链完成，确认收入5/支出7、12条来源，页面抽屉已验证。不是重放第1次请求，不覆盖其结果。

旧Run保持原持久结果，因此中间版本的B结果仍可能显示修复前的未知方向；最终准确样例在 `资金线索响应样例.json`，不要用旧Run验证新投影版本。

## 4. 源码回归证据

- 后端初始全量：890 passed /24 failed。失败集中于隔离测试镜像缺少Node/Bun；挂载正确工具及运行权限后相关四个模块70 passed，包含原失败用例。此记录不冒称最终源码完整套件重新全通过。
- 后端定向：维护恢复和运行池66 passed；最终收支映射、空时间及结果投影12 passed。两次有重叠，不相加为独立用例总数。
- 部署/Worker/恢复/配置最终定向：86 passed，另34 subtests passed。
- 新增附件/恢复/详情测试最初4项通过，后又增加维护退出及credit/debit两项；均包含在上述定向运行。
- 前端 `bun typecheck`、`bun run build` 通过；已有构建警告包括基础tsconfig与较大chunk，未当作构建失败。
- 真实HTTP定向11项通过，详见 `HTTP联调结果.json`。代码测试、HTTP结果与浏览器交互分别记录，不能相互替代。
- 全量部署套件不是全绿：原历史发布封存检查与当前候选源码存在source_drift，部分隔离运行曾因Git/宿主ACL/Python版本不完整失败。已修正测试替身并跑过相关功能用例；没有修改旧封存数据或放宽检查伪造通过。

## 5. 明确遗留

1. 390像素原有导航背景布局需要前端后续响应式专项修复。本轮未动CSS。
2. 旧车辆Run缺少批准的记录级claim，仍可能partial/0节点。当前显示真实原因，没有用模型文字或示例图补齐。
3. 复杂自然语言请求仍可能进入范围澄清；本轮未扩展TaskSpec对象解析规则。应另行定位误拒绝，而不能静默改用场景默认对象。
4. 上传partial/no_text/超限文件、长期SSE断线、负载、完整备份恢复及真实业务数据源未执行。
5. 本轮未增加P2算法产品页面，也未改变资料权限或模型核对门槛。
6. 历史发布封存清单需为本轮候选建立新的独立发布记录，不能把旧阶段清单直接当作本轮全量交付验收。

## 6. 交付文件

- `前端联调接口说明.md`：新增字段、状态和兼容语义。
- `部署维护与回退说明.md`：唯一匹配的部署脚本、恢复路径和备份边界。
- `openapi.json`：从最终运行版本导出，共118条路径。
- 响应样例、HTTP检查、图谱交互、镜像/源码/CSS核验、模型预算与截图。
- `SHA256SUMS`：逐文件摘要；不含凭据、Cookie、私密配置或用户资料正文。

浏览器验证全部在服务器本地通过正常登录进行。跨主机导出认证Cookie的方案被自动审批拒绝后未执行，没有转移认证材料。
