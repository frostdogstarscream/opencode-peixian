# 第五轮定向验收记录

日期：2026-09-22。站点：https://36.134.45.38:19460/ 。范围：合成资料、两个现有普通测试账号、小规模定向联调；没有真实资料接入与并发压测。

## 1. 已完成修改

- 复杂请求的有限规范化与场景保留，不删除未知对象、范围及冲突方法。
- Run 增加 outcome，将执行终态与取数结果分开；页面复用既有样式展示具体下一步。
- 车辆事实由平台确定性检查支持进入可信结果/图谱；不依赖模型逐条选择摘要，历史结果不回填。
- 新增定向回归、OpenAPI 字段、两个账号线上与浏览器联调。

## 2. 源码验证

| 检查 | 实际结果 |
|---|---|
| 后端相关回归 | 282 passed，2 warnings |
| 前端测试 | 77 passed，0 failed；2230 assertions，12 files |
| 前端类型检查 | 通过 |
| 前端构建 | 通过；保留既有 tsconfig/chunk 提示 |
| git diff --check | 通过 |
| 部署源码一致性 | 运行镜像内 89 个 Control/shared Python 文件 SHA256 与源码逐一一致 |
| 样式 | CSS SHA256 与上一部署一致，未修改样式表 |

最初扩展回归为 218 passed/1 failed：既有测试假定仅摘要选择可批准车辆记录，与本轮新代码证明机制冲突。将该用例明确为无新版本标记的旧快照，保持历史边界；新证明机制、图谱和篡改拒绝另有正向/反向测试。调整后完成上述最终回归。

新增用例覆盖：复杂请求、礼貌表达、原文冻结/重放、未知姓名/范围/注入/冲突方法拒绝、真实编译器与持久回执、车辆无模型摘要也可生成图谱、旧快照不回填、句子/来源/时间/快照/回执/统计篡改拒绝、执行/资料状态区分、跨账号 404、场景追问继承。

本轮不是全仓库后端、全部部署脚本或大规模性能验收。

## 3. 线上定向请求

| 账号及用例 | Run ID | 结果 |
|---|---|---|
| A：复杂资金问题，附页面测试附件 | 3e78f1d45ebf4a90a54416b424a57f77 | completed；涉赌 new_query；仅 funds，1 次新取数；12 条来源记录、12 条批准事实；图谱 13 节点/12 边；data_ready |
| B：当前盗窃场景车辆记录 | 30e3c8c778574a1ea1dce54c4d325cc3 | completed；盗窃 new_query；仅 vehicle，1 次新取数；原返回22条，按冻结对象/范围取得8条；8条批准事实；图谱2节点/8边；data_ready |
| A：继续解释收支、不重新查询 | 2e336643a2794d32b701c4c2d68eae62 | completed；explain_existing；涉赌场景不变；关联原资金 Run；新增取数0；historical |
| B：未知对象最近30天车辆 | 969375aaff964bd080cb731d21e72f5d | completed 仅表示澄清结束；needs_input，queried=false，新增取数0；无记录，不映射到默认对象 |

四条用户级请求均只提交一次。当前统一账本已用12/40，本轮占4，剩余28；失败/未知同样计数。不支持对象的请求本地澄清，没有触发取数；用户请求数不等同付费模型实际调用数。

每个 Run 的 detail/task/result/graphs/report 均验证其他账号 404；存在的具体图谱也验证跨账号404。再次读取持久结果完全相同。Markdown 报告可下载。

## 4. 页面验证

- A/B 实际登录，普通用户可从中文资料状态和下一步提示继续操作。
- 复杂资金、车辆、范围补充三个会话的1366宽度截图已保存。
- 刷新后页面按既有行为回首页；重新打开原历史会话后，恢复相同资料结果。不能据此声称刷新自动保持选中会话。
- 车辆图谱 canvas 可用；缩放、拖动、路径高亮、大图弹窗开关通过。
- 短暂断网恢复后保持同一 canvas；没有自动重新发起资料查询。
- 检查的页面无 pageerror。
- 本轮未重做1920/390宽度、深色模式和全部导出交互的完整回归。

## 5. 未关闭的限制

人工阅读车辆报告发现英文开场，以及部分自由说明没有得到逐项来源支持。现有摘要检查状态为 conflicted；其中也包含固定句核验及保守词语规则的影响。因此不能将模型自由说明标记为全部通过。

8条车辆事实及图谱来自独立代码核对。上述未支持的文字没有因这次修改提升为可信图谱事实。后续应独立改进中文约束与基于事实的叙述校验，不宜用删除失败提示或放宽事实验证来处理。本轮没有为此自动重试模型。

旧车辆 Run 缺少新证明版本时不补建图谱。新查询生成的结果使用新规则；历史已保存结果继续可读。

维护诊断 safety_sync_failures 为进程累计9（发布前6），是发布期间增加的安全同步失败计数；最终 recovery_required=0、security_pending=0、normal，A/B ready/open，Worker active。不能把该累计数写成零，也不能替代持续稳定性验证。

## 6. 结论

范围解析、资料状态提示、车辆可信事实至图谱与两个账号定向接口/页面联调通过。模型中文与自由叙述一致性仍有缺口。未开展真实业务资料、持续负载、故障恢复全套演练；这些未执行项不记为通过。
