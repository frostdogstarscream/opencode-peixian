# 受控中文说明与盗窃接口契约核对：修改及验收回执

日期：2026-09-22。代码已发布到现有测试站点，核心受控回答链路通过；390 像素窄屏可读性未通过，不能将本轮表述为全部验收完成。

## 1. 版本与修改范围

- 基线：9d15fe5c89b31fa8506df00ae528213417deab54。
- 实现提交：a0e1e556ce89f3403d9673a94279f609156950fa。
- 镜像：agent-platform-control:alignment6-a0e1e556ce89。
- 镜像 ID：sha256:599964b247e6a09d89ec35f85748cab82078bb9f1348dedce35bd0c20cd32ad4。
- 数据库保持 schema v9；未修改供应方接口、插件、个人 Skill、Agent/Gateway/Relay 镜像及账号配置版本。
- 本轮修复新策略 Run 的资料查询、历史解释和范围澄清；普通开放式聊天不在强制事实模板范围内。

代码新增 controlled_answer 模块，复用原 Claim 核验、结果投影、加密快照和持久 Run。模型选择失败时仍可使用代码批准的事实，原模型自由说明仅保留内部诊断。聊天正文、Result V2、Markdown/HTML 报告使用同一份持久中文 answer；临时正文缓存及流式路径同时过滤。前端增加契约类型与现有区域接入，未修改样式文件。

## 2. 确定性回归

| 项目 | 结果 | 证据边界 |
|---|---|---|
| Control 选定回归集 | 272 passed，2 warnings | 覆盖受控回答、Run、来源、历史、OpenAPI、账号隔离、正文缓存、EventHub、流清理等；不是全仓库测试 |
| 前端测试 | 81 passed，0 failed；13 个文件、2236 项断言 | 包含版本与来源绑定拒绝、已有功能回归 |
| 前端类型检查和构建 | 通过 | 保留已有构建警告，不等于浏览器验收 |
| 零记录、部分资料、取消/失败、未知结果 | 确定性用例通过 | 本轮没有对每种状态另做真实模型故障注入 |
| 伪造引用、错误对象/数字、同框/同行、车辆方向及金额 | 确定性用例通过 | 代码模板仅读取受保护字段；不认证任意自由文本 |
| 新策略流式正文过滤 | 缓存、流与消息回归通过 | 实际联调另做执行期间轮询；未声称进行网络竞态压测 |

后端日志保存在服务器本轮受限验收目录；交付包不包含密钥、数据库或模型原始诊断正文。

## 3. 两账号定向请求

| 账号 | 场景 | 结果 | 本轮取数 |
|---|---|---|---|
| A | 资金查询 | 12 条记录，5 项中文摘要，answer=ready | 1 次 |
| B | 车辆查询 | 8 条车辆记录，5 项摘要，图谱 8 条来源边 | 1 次 |
| A | 资金历史追问 | 使用冻结的来源 Run，5 项摘要 | 0 次 |
| B | 不支持的范围 | needs_input，明确尚未查询 | 0 次 |
| A | 车辆查询 | 8 条车辆记录，5 项摘要，图谱 8 条来源边 | 1 次 |
| B | 资金查询 | 12 条记录，5 项中文摘要 | 1 次 |
| B | 资金历史追问 | 使用冻结的来源 Run，5 项摘要 | 0 次 |
| A | 不支持的范围 | needs_input，未拿默认对象代替输入对象 | 0 次 |

八条请求都正常结束。六条有模型参与的执行，其原模型 narrative 诊断仍为 conflicted；这没有被改写为模型核验通过。新的公开 narrative.text 为 null，最终正文来自已批准 Claim 的中文模板。两条范围澄清由代码处理，没有启动模型。

逐项检查 answer 的 Claim、source_run_id、source_ids 与可信结果一致；公开消息只返回该 Run 的受控正文。八条结果均完成 HTTP 复查、Markdown/HTML 报告检查、另一账号访问拒绝检查。另核对代码模板对记录方向、金额、时间和对象字段的表达；没有将模型原文提升为图谱或中文事实。

请求预算：执行前 12/40，本轮登记 8 条用户级请求，执行后 20/40、剩余 20；失败/未知也按规则计数，本轮没有自动重试。用户级请求数不等于底层模型 HTTP 调用次数。

## 4. 页面、历史与发布核对

- 对六个会话分别采集 1366、1920、390 宽度截图；脚本未发现页面 JavaScript 错误，刷新后重新选择会话可恢复相同结果。
- 桌面页面完成中文说明、资料状态和来源展示检查。车辆图谱画布、缩放拖动、来源路径、大视图及断线恢复检查通过。
- **390 像素未通过人工可读性检查**：左侧布局仍占用约 215 像素，聊天区域被挤压。自动“无全局横向溢出”检查不能发现这一问题，不作为手机验收通过证据。
- 本轮没有修改 CSS，运行样式文件及发布前哈希一致：index-DI4D4ule.css，SHA256=6939df9f518d754748bf99bb3b524f66b3aa47cd4d957630c4d8ed3da5eadd1b。窄屏布局单独列为后续前端修复项，不通过本轮重写现有样式。
- 四条发布前 Run 的完整 Result 响应及 Markdown 报告逐项对比一致；没有用新规则补写旧摘要或图谱。这是选定历史样本核对，不是全部历史数据审计。
- 运行 Control 的 90 个 Python 文件与提交后源码逐文件 SHA256 一致；镜像标签、镜像 ID、源码 revision 相符。
- 复核时 Worker active，A/B ready 且 gate=open，配置版本分别为 54/27，维护状态 normal，待恢复和安全待处理均为 0。
- safety_sync_failures 是进程累计诊断值，本次复核为 12，不表述为零故障，也不将累计计数等同于当前仍阻断。

## 5. 新接口文档审阅

交付《盗窃接口契约矩阵》，逐项区分文档明确、文字冲突和待供应方确认。包括 RSA/明文密码冲突、认证边界、公里/米、处警/案发时间、两套夜间规则、坐标系、分页覆盖、限定抓拍人群、预警计数、跨小区参数、夜间明细引用及轨迹稳定标识。

本轮未调用真实供应方接口、未开发真实适配、未读取真实姓名/身份证/轨迹。原供应方文档保持不变；个人嫌疑评分、排序、行为关注优先级及完整人工复核工作台不纳入实现。

## 6. 发布、回退与未完成项

发布前保存源码、控制库、匹配部署配置及旧镜像，停止旧写入和 Worker 后发布匹配 Control/静态资源，再恢复并核对两个账号。没有数据库版本迁移，没有覆盖个人 Skill。

回退使用保留的上一匹配镜像 agent-platform-control:alignment5-a7f639ce2e3e；先确认无活动 Run，再按现有维护流程切换。保留发布后的新增数据，不用旧数据库覆盖。旧版本不具备本轮正文过滤策略；如回退，不能继续宣称受控中文回答已生效。

未完成/未执行：390 像素布局修复、真实供应方接口联调、并发/持续负载、全部历史记录审计、普通开放式聊天的语言保证。以上不记为通过。

源码和文档分别保存服务器本地提交，不自动推送 GitHub。实现提交与文档提交分开记录，运行镜像对应实现提交，不因追加文档改称为新的运行源码版本。
