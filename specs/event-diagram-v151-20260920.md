# 扩充资料事件图适配

## 版本边界
保留原1.4.0固定核对数据。扩充的186条记录不再复用demo1003/demo1001；作为不可变插件1.5.1发布，场景快照demo1005、资料快照demo1004，观察范围2026-09-10至2026-09-17（结束不含）。
本轮未将复用旧快照的1.5.0加入可信版本白名单。历史Run保持原数据；旧版本不自动补图。1.5.1源包位于deploy/peixian/examples/peixian_synthetic_records/releases/1.5.1；不覆盖同事正在编辑的plugin目录。

## 实现
复用确定性事实编译引擎，新增scenario_data_v151.json及完整/部分响应固定表。版本选择按场景与两套快照精确匹配。窗口展示改为实际范围，保留每页20节点、来源和时间关系。来源模块记录总数186不等于两个场景各自命中数量。
生成命令：node deploy/peixian/examples/peixian_synthetic_records/build_facts_projection_v151.mjs。
数据服务与插件使用同一份冻结JSON；以独立服务发布目录和可回退systemd覆盖项切换，不覆盖原数据文件。

## 验证
后端443项通过；两场景×1366/1920/390六组浏览器测试通过，增加分页跳转、页面二来源核对；PNG/SVG均可导出。没有修改前端业务源码，静态资源保持上一轮版本。
真实模型、账号恢复和最终镜像结果另写发布回执。模型预算沿用19/40，首批2次新请求，不自动重试。
