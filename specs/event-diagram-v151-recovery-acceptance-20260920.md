# 事件脉络图 1.5.1：恢复与联调回执

日期：2026-09-20。工作区：frontend-alignment。本文接续 event-diagram-v151-20260920.md，并取代 event-diagram-v151-pending-recovery-20260920.md 中“B 尚未恢复”的当时状态；不覆盖历史记录。

## 交付结论

账号 A、B 已恢复 ready，入口开放，恢复标记清除。A applied/desired 为44/44，B为23/23，均接入不可变插件1.5.1。未手工修改控制数据库，未删除持久卷、会话或文件。超级管理员与管理员仍无普通用户运行环境。

代码、186条冻结资料和发布已完成；两场景真实模型验收未全部通过。B盗窃场景完成并生成30节点事件图；A涉赌场景终态失败，没有可信事件图，不应标为通过。本次没有重发失败用户请求。

## 扩充资料与图形身份

插件1.5.1；场景快照demo1005；资料快照demo1004。观察范围为北京时间2026-09-10至2026-09-17（结束不含）。七模块数量为资金36、通话36、同框观测22、综合18、夜间32、车辆22、关联20，共186条。服务器实际七模块响应已与冻结JSON逐项比较一致；无认证请求被拒绝。

保留旧版本事实表和旧Run，不用最新夹具补写旧结果。1.5.0复用旧快照的组合没有加入可信白名单。页面取数、代码事实表及资料服务对应同一发布；图形不新增模型调用。所有资料仍为完全虚构数据。

## 经用户明确允许的B恢复

1. 停止宿主Worker，取得其独占文件锁。
2. 核对B的Agent/Gateway/Relay容器身份，确认全部停止且无对应Docker变更进程。
3. 保存完整旧宿主变更记录和核对证据，再将过期宿主记录标记为已核对空闲。
4. 恢复Worker，使用平台正常暂停、恢复API完成状态流转，没有绕过API手工编辑控制库。
5. 初次暂停后发现旧失败执行尝试仍未闭合，恢复仍被保护性拒绝，随后修复下述两个代码问题，再次通过正常暂停/恢复完成B恢复。

相关诊断和备份仅保存在服务器受限发布目录，包括b-host-reconciliation.json、升级前控制库副本和部署配置；不将凭据或数据库纳入Git。

## 两项生命周期修复

- f139f52e7：只有确认完整停止且无变更进程的暂停完成，才结清同Runtime较早失败/取消任务中未闭合尝试，关联暂停和观测回执。既有结果、其他账号和后续任务不改写。
- 53db9c39d：已经显示paused但仍有未结清尝试时，不能短路返回“无需暂停”；进入正常核对流程，避免永远无法恢复。

增加针对真实停止证据、旧尝试结清和暂停短路的回归。两项修复均通过后才发布最终Control。

## 匹配运行版本

- Control源码：53db9c39dc03671ae788608aeacf04a61d42125a。
- 镜像：agent-platform-control:event-diagram-v151-final-20260920。
- 镜像ID：sha256:660368f2968a4e91b6186f0c284e9b5636ca2a56dc9c49b7c59f73c8bf18e057。
- 数据库：schema v6，无迁移。
- 本次恢复镜像沿用已有事件图前端静态资源；没有把工作区同事后续未提交的前端文件自动构建上线。工作区不干净是已保留的协作差异，不能将HEAD当作整个站点所有层的唯一构建来源。
- 本地提交，不自动推送GitHub。

## 验收证据

| 范围 | 结果 | 说明 |
|---|---|---|
| 最终后端全量测试 | 445 passed，2项依赖弃用警告 | final-tests.log；96.97秒 |
| 扩充资料隔离浏览器测试 | 6组通过 | 两场景×1366/1920/390，确定性测试数据；不是6次真实模型请求 |
| 真实B证据持久查询 | 通过 | 两次GET相同；30节点，2页20+10；快照demo1005/demo1004 |
| A访问B的Run及证据 | 404 | 正常账号API验证 |
| B Markdown报告 | 通过 | 包含Mermaid源码与数据性质说明 |
| 模型A：涉赌 | 未通过 | failed / finished_without_result / no_terminal_result；有完成和失败的工具记录，未形成可确认结果 |
| 模型B：盗窃 | 通过 | completed，evidence complete，diagram ready |

真实请求：本次2次，累计21/40，剩余19次。A失败计数，无自动重试。浏览器重读同一结果不发起新的模型请求。一个Run内部多个工具尝试与平台重发用户请求不同；本报告不将工具次数当作用户级请求数。

A Run为c7d9b5d79d0a4cccb1cb97705d2641df；B Run为5e503091fbec4690a05f8646b39294b2。公开执行步骤只提供脱敏错误，当前不能确认A的工具错误根因。尝试解密Gateway内部服务密钥并读取原始执行错误的方式被自动审批拒绝，未执行；没有改从其他通道读取同一内部正文。本次保留失败证据，不把它归因为资料服务错误或模型单一原因。

## 页面验收和限制

真实浏览器脚本只检查diagram ready的既有Run，并要求至少有一个成功Run，不能以空集合通过。本轮实际成功集合只有B；因此B页面结果不代表A真实链路通过。

B的真实页面在1366、1920、390三种宽度均通过分页、PNG/SVG下载、来源抽屉、大图弹窗、Escape关闭与焦点恢复检查，pageerror为0。实际视口与弹窗截图已保存。人工查看1920桌面与390手机弹窗可识别日期、对象和事件；手机需要图内横向浏览并列节点。

视觉验收为部分通过：390宽度的展开资料侧栏及固定输入区挤占主图区域，需后续与前端同事整合折叠策略；不以元素存在或可点击替代完整可读性通过。

早期长组件截图受滚动容器和固定栏遮挡，不能作为视觉通过证据；交付改用viewport与modal截图，并单独保留PNG/SVG导出。页面交互与视觉可读性分开判断。没有执行大规模并发、长期负载或真实业务资料验收。

## 复现和回退

前端真实浏览器脚本：packages/peixian-console/tests/event-diagram-live.mjs。可通过PEIXIAN_DEPLOY_ROOT与PEIXIAN_ACCEPTANCE_DIR指定部署目录及已有smoke-results.json所在目录，使用受限private.json中的账号凭据进行普通登录；不得把该文件上传Git。脚本不提交消息，不修改插件或账号配置。

后端测试在services/peixian-control目录执行pytest tests -q。图形确定性测试与生成方式见前一份集成说明。

需要回退时保留新会话、文件及版本记录，使用已保存旧Control镜像和服务发布目录；不得回填旧数据库覆盖新增数据。新增图形字段允许旧客户端忽略。旧插件授权配置通过平台版本流程处理。恢复缺陷修复宜保留在回退候选中，避免旧未闭合责任再次阻止恢复。

## 尚未关闭

1. A涉赌真实模型工具错误及no_terminal_result根因，需在允许的诊断范围内进一步排查；没有重试。
2. 两场景全部真实链路验收仍缺A成功样例。
3. 新的模型请求必须显式安排并计入40次账本，失败与未知继续计数。

4. 390宽度主页面的空间分配仍需调整；大图可用不等于手机主页面布局全部通过。

## 验收证据SHA256

```json
{
  "final-tests.log": "42191081a79f59662d9b69b403d4576da5b5be23d0d8f513e92ea1608709e73a",
  "live-browser.json": "a82de01a2fb646f82abf5c914468121eeb6ad055d3d25560c569902101b15932",
  "smoke-results.json": "475d813f31e746abdfd145cf619e82bac9659cfbd4bb1dd874ad316a9aa4dae6",
  "model-ledger.json": "28cd40dba10a19e8a1d68a2582a0a46bb2099bf52766366aa018d3f6dfd11d05",
  "service-verification.json": "00c427ab42d1414921de39100c2957b636c8c9c8df7bdcd74346e5a26b89f244",
  "modal-alignment-b-1366.png": "82ec00c2b723ad94986f74ce27f48a0feeb1bd8d02f21c0b7fa86e5f0d4d04c7",
  "modal-alignment-b-1920.png": "634b8aca831ed89af18d7a9f13337988fe85fe600ed4adea8a6bb0df5e94dbb4",
  "modal-alignment-b-390.png": "cae4763201a4f1e2f8ed0f4cd9406470a14f77549afb4a683b6557d3f84da6ea",
  "viewport-alignment-b-1366.png": "efd4b10824a64516f1db3a1db6db89b2cfd19194f208c1c4f13e276551f2ca81",
  "viewport-alignment-b-1920.png": "9e3e266f31b2fdc0ed1ee0765843b92f109254a9f9361db64799806f1504e00e",
  "viewport-alignment-b-390.png": "f4d35c5d2d9e28aef3a8d8c533482387963907446a68677c7b9045afca51d20c"
}
```
