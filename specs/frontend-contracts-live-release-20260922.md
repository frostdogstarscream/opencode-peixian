# 2026-09-22 后端接口发布与真实联调回执

## 发布身份

- 实现提交：`7e5de36f7abd1113249d33a26b90b4e1a74cc4fe`，分支 `codex/frontend-contracts`。
- 接口基线：`1241f97120556034d6deb63d23d010a7cc790ee2`。新增最小修复：配置生产端携带技能版本，避免已安装官方方法被误判待生效。
- 站点：既有 HTTPS 19460，同源 `/api/console/v1`。schema 已从6离线升级到9。
- 最终Control：`sha256:9b022ed039ba89c3da5405039e4800400d9d18d3849f56dbe379ea7ca70f7022`。
- Gateway/Relay：`sha256:31691088a0cbe8cac7c9532d0c3af1f0ae744dd0de35dc89e407291afa392443`。
- Agent：`sha256:7eb5b8e2d442ca1dce7f18acc77170533dbf4abee8a5a5ab46cf12221607de48`。
- Agent/Gateway实现与6f6790221相同，Agent复用已编译产物，经源码差异核对，并为匹配集合记录当前源码身份；没有声称本轮重新编译Agent内核。
- 前端同事最新701个静态文件逐文件SHA256完全一致；不修改前端源码。曾发现基础镜像59个旧资源残留，最终镜像中已移除并再次核验。
- A、B最终均ready/open，无恢复责任；8个本平台容器健康，未结束Run/环境任务/执行尝试均为0。其他部署未更改。

## 备份与迁移

升级前执行7个卷、配置发布、Worker状态、匹配密钥和TLS材料的完整停写备份；保存在服务器受保护目录，不随本包分发。最终备份恢复到空目录并逐文件校验。未启动另一套完整恢复容器，不能称为完整运行恢复演练。

控制库副本迁移、重复初始化、升级前后副本恢复通过。真实库迁移保留原109条Run和原业务表内容。旧schema6镜像实际连接v9副本时被拒绝。旧镜像不得直接接管已升级的库。

备份恢复启动后出现维护409，是组件身份变化触发恢复核对；通过repair_only完成核对后使用正常暂停流程，未手动清除任务。一次最终备份尝试因HTTPS代理未停而被工具拒绝，停止本平台代理后在新目录成功完成，失败记录保留。

## 源码修复及测试

真实灰度暴露 `runtime_spec` 未输出Skill版本，而官方方法要求版本一致。补充version字段，并用真实配置生成函数验证官方方法可用；内容版本变化后旧快照仍须拒绝。相关回归80 passed，2条依赖弃用警告。

上线旧配置原revision不可覆盖；管理员apply是同版本重试，不人为删除不可变目录。经哈希确认A/B资金方法是未修改官方副本后，保留正文和依赖原样重新发布，Skill版本1→2，配置A53→54、B26→27，其余个人技能不覆盖。

上一轮904/79/29记录保持原范围，本次没有再重复整个904项，也不把各轮数量相加。

## 真实请求与接口结果

本轮账本累计5次用户级验收提交：首次409受理拒绝也计入额度；修复后4次新会话实际执行成功，没有自动重发失败请求。当前40次上限中占用5次，余35次。本次没有调用真实资料服务或并发压测。

| 账号/助手 | 合成任务 | 202受理耗时 | 执行 | 图谱 |
|---|---|---:|---|---|
| A/涉赌 | 资金整理 | 342.39ms | completed | ready |
| A/盗窃 | 夜间活动 | 212.88ms | completed | ready |
| B/涉赌 | 同行核对 | 365.08ms | completed | ready |
| B/盗窃 | 车辆记录 | 232.98ms | completed | partial、0节点 |

四次都保存了真实Run、步骤和可信摘要Part。车辆查询确已返回资料，可信结果只形成computed/gap条目，没有approved fact条目；图谱拒绝从统计或模型文本猜测关系，因此保持partial。该项不计为完整图谱通过；下一轮需核对事实批准与来源投影链路，不能仅改前端成ready。

以上4次耗时只代表少量样本，不是p95、并发性能或稳定性承诺。受理接口不等待模型回答；返回前仍执行会话归属、文件预算及运行协议检查。

23项真实HTTP检查通过，覆盖图谱目录/分页/节点/展开/路径/白名单算法、修订冲突409、跨账号Run/步骤/证据/图谱/报告404、管理员图谱403、Markdown/HTML报告、上传文本后单文件ready、文件跨账号404、SSE建立与客户端关闭。另验证缺失CSRF403、无效游标422、退出后401。

前端需直接使用同包OpenAPI。`live-http-examples.json`为合成业务数据的实际HTTP响应；`live-message-bindings.json`摘录实际消息关联及工具公开字段，省略正文，不能当完整messages响应。

## 未完成事项

- 尚未重新执行真实浏览器页面联调、手机布局、图谱画布交互与刷新恢复。前端同事之前的合成响应浏览器验证不能代替此次真实联调。
- 车辆场景图谱partial/0节点为明确缺口，不将Run completed等同于图谱完整。
- 本次未复测真实partial/no_text/超限文件、全取消与unknown故障注入、SSE断线高峰、完整恢复容器启动或真实数据源。既有确定性测试证据与本次HTTP实测区分。
- 不新增逐token可重放流；仍是change通知与GET快照。P2后端可调用，但前端未接入算法页面。
- PR-9C全套关账门槛未全部执行，保持pending；此次完成后端发布与所列小规模联调，不代表PR-9C全部关账。

## 运维与回退

运行平台配置已固定最终镜像ID。当前generated Compose包含A/B三个功能白名单：PX_TASKSPEC_V1_UIDS、PX_MULTI_AGENT_V1_UIDS、PX_TRUSTED_RESULT_V2_UIDS；没有长期迁移许可开关。容器重启会保留这些环境变量。后续若重新执行平台render/up，须重新应用已保存的feature-flags.json后核验，不能用默认重渲染丢失白名单。

Worker systemd使用候选仓库脚本，保留原前端工作区。后续替换源码或部署前必须协调，不能使用旧schema6镜像。必要回退使用兼容schema9的版本；恢复升级前全套备份必须到空目标，先保留升级后新增数据，不能只恢复控制库覆盖新增内容。

本包不含凭据或运行备份。源码和回执仅服务器本地提交，本轮未推送GitHub。
