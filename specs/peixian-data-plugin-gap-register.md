# 沛县公安数据插件接口缺口登记

状态取值：`Mock 可联调`、`等待正式契约`、`等待接口`、`正式联调完成`。

| 能力 | 当前状态 | 已有材料 | 正式联调前置条件 |
|---|---|---|---|
| 人员户籍、房屋、单位、住宿、铁路、网吧和车辆档案 | 等待正式契约 | 字段和路径文档 | 正式根地址、认证、分页、错误和脱敏测试集 |
| 家庭关系 | 等待正式契约 | 请求及响应样例 | 正式地址、合法参数 Schema、历史关系口径和健康检查 |
| 涉案人员 | 等待正式契约 | 请求及响应样例 | 合法 JSON、人员角色、分页、错误和限流 |
| 涉警人员 | 等待正式契约 | 请求及响应样例 | 分页、时间跨度、人员角色和错误契约 |
| 人员轨迹 | 等待正式契约 | 请求及响应样例 | `track_type` 类型、地点字段、最大时间跨度和数据量限制 |
| 在库人员清单 | 等待接口 | 仅需求描述 | 正式接口和授权范围 |
| 同住址人员 | 等待接口 | 仅需求描述 | 由身份证查询地址及同址人员的正式接口 |
| 人像关系 | 等待接口 | 仅需求描述 | 人像命中、相似度、共同出现及明细接口 |
| 资金记录 | 等待接口 | 需求称有表格模板，但目录中未发现 | 正式契约、账户类型、时间和交易方向定义 |
| 通话记录 | 等待接口 | 仅需求描述 | 主被叫、渠道、时间、时长和明细接口 |
| 车辆共同使用 | 等待接口 | 仅需求描述 | 车辆、人员、共同使用次数和明细接口 |
| 夜间高频统计 | 等待接口 | 仅需求描述 | 人员、车辆、点位聚合及明细接口 |
| 手机号、车牌、支付账号反查 | 等待接口 | 仅需求描述 | 多标识实体解析和稳定实体 ID |
| 关系排名 | 等待正式规则 | 文档明确待确认 | 数据源权重、时间衰减、去重、并列和缺失值规则 |

所有现有能力均已完成 Mock 契约和插件框架，但没有任何一项标记为“正式联调完成”。接口方补充资料后，应更新 OpenAPI、字段映射、适配器、测试报告和本登记表，再提升插件版本。
