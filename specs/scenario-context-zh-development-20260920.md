# 场景续接与中文回答开发回执（未发布）

## 完成内容
- 后端按明确文字、显式场景 Skill、同会话已确认场景解析；冲突返回可操作中文错误。
- 持久 Run 快照保存有效 Skill、场景来源、配置版本和重置代次，保留原请求参与幂等，不因重放重选场景。
- GET/DELETE 会话 context，清除记录为既有审计表中的持久事件；原子校验活动执行和身份，重复清除请求返回原回执。
- 升级前无 Run 的会话仅由受信资料证据恢复场景；不读模型自由文本决定场景。
- 平台模型 system 统一简体中文；业务枚举和报告状态中文化，术语代码保留。
- 场景标记和清除 UI 独立于本次 Skill/文件选择，跨会话及清除后的迟到响应受到代次保护。
- 两个事实整理 Skill 源文件升级为 2.1.0，并保留旧版内容摘要的场景映射。

## 实际验证
| 层次 | 结果 |
|---|---|
| Control 全量测试，独立无网络 Python 3.12 容器 | 430 passed，2 项依赖弃用警告 |
| 前端 Bun 测试 | 64 passed，0 failed |
| 类型检查、Vite 构建 | 通过 |
| 隔离浏览器 1366/1920/390 | 场景显示、清除、刷新与中文状态通过，page_errors=0 |
| 真实模型请求 | 本轮 0 次，未执行 |
| 线上版本升级 | 根据用户最新要求暂不发布 |
| 两账号 Skill 更新和真实工具联调 | 未执行，待发布窗口 |

初次宿主 Python 3.11 测试与首次全量容器测试分别受 Python 版本、生产镜像默认 v6 开关影响；已在 Python 3.12 中由各测试夹具自主选择版本重跑通过。隔离浏览器使用合成接口，不证明线上模型行为。

## 发布状态
候选镜像：sha256:5a56d23fdf2b6d1a0946346a1fa232169667288ca46e7a40a9060d38d0670d8e
线上镜像由前端同事更新为 frontend-skill-background-20260920；本轮未替换。
发布尝试在备份导出阶段发现容器被外部部署替换，尚未变更平台配置或停止 Worker。用户随后确认“暂不发布，先交付源码和测试”。
未更新个人 Skill、模板或运行配置，未执行付费模型请求。此前验收账本合计14次，本轮新增0次；重新联调前须重新核对剩余额度。

## Git 与后续步骤
前端已有改动检查点：ed3ccf04311087213ebb78a59e3f59d86da031b6。
本轮功能另行本地提交，不推送远端。未纳入既有业务适配器、部署脚本等无关未提交改动。
后续：确认发布窗口，备份当前运行版本，重新核对候选源码和最新前端差异，再发布匹配镜像；按账号逐个更新未被个人编辑的受控 Skill，检查 Worker 注册和账号环境；最多8次真实请求验证续接与语言，失败不自动重试。

## 已知边界
语言由系统规则约束，不能承诺模型绝不偏离。当前仅固定合成场景，不支持真实身份证查询。对身份证号、显式对象变化、常见自定义日期范围进行代码拒绝；任意自然语言的全部对象表达不构成通用解析能力。
上下文重置依赖持久审计事件；未来引入审计清理必须保留或迁移这些事件。旧镜像不支持本能力，不能将回退描述为功能等价。
