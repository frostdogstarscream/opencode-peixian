# 场景续接与中文回答发布验收记录（2026-09-20）

## 发布版本
- 用户确认发布后，使用正式 platform-manage up 发布，入口 https://36.134.45.38:19460/。
- 功能源码提交：4c600c16a00d9999ff4a4fdd0bf47e5f4d69f10e。
- Control/前端镜像：sha256:5a56d23fdf2b6d1a0946346a1fa232169667288ca46e7a40a9060d38d0670d8e。
- 发布前镜像：sha256:26501e79e6672ff44c9caed70df5f085028732e716ce20c466aa256f1a814772。
- schema 保持 v6；没有升级 Agent/Gateway/Relay。保留同事此前背景和页面改动、无关工作区修改及用户数据。
- Worker 已重新注册；两个账号管理网络已恢复连接；控制台与 HTTPS 健康。

## Skill 与环境
- 只更新内容与旧受控源文件完全一致的两个事实整理 Skill，版本内容为2.1.0；账号A先发布并确认生效，再更新B。每账号两个，共4个。
- 原ID、名称、启用状态、依赖保留，旧内容通过原有历史机制保留；其他个人副本和模板未覆盖。
- A desired/applied=39/39；B=20/20；两账号 ready。
- B 发布前已存在持续延后的暂停任务，实际阻塞为1条等待问题。用户明确授权结束后，拒绝该旧问题、等待暂停完成，再经管理接口恢复环境。未删除会话或文件，未直接改写状态数据库。

## 本轮实际验收
| 项目 | 结果 |
|---|---|
| A明确涉赌+受控Skill | completed，取得结构化核对结果，中文结束说明 |
| A追问资金、skill_ids为空 | completed；冻结快照source=inherited，自动选回涉赌Skill，没有再次要求选场景 |
| A文字切换盗窃、skill_ids为空 | completed；冻结快照为盗窃和对应有效Skill |
| B新会话未确定场景 | completed；中文提供两类场景选项，不要求内部编号 |
| 平台语言约束 | 4个Run冻结payload均含简体中文system规则；4次最终说明为中文 |
| 不支持身份证查询 | 422 scenario_scope_unsupported，沿用涉赌提示但明确尚未查询，不提交模型 |
| 跨账号访问A会话上下文 | B返回404 |
| 场景清除与重新登录 | 清除后generation持久；重新登录仍scenario_id=null |
| 实际线上1366/1920/390浏览器 | 场景标记可见，盗窃场景正确，page_errors=0 |
| 4个更新Skill加载测试 | 均返回ok=true |

本轮真实模型请求4次，失败或未知不自动重试；此前验收账本14次，本任务累计18/40。普通用户自行发起的站点请求不混入该验收账本。没有并发测试。两条新建合成测试会话保留，A验收会话已清除当前场景用于验证重置。

此前已完成Control 430 passed、前端64 passed、类型检查与构建、隔离三宽度场景清除回归；本轮未重复宣称重新运行该全量测试，而是补充上述线上证据。

## 证据与回退
私有运维目录：/srv/peixian-alignment-20260917/scenario-release-20260920-101430。
含发布前platform配置、SQLite一致备份、两个账号Skill备份、模型账本、脱敏快照检查、线上浏览器截图与报告。数据库与账号配置备份仅留服务器私有目录，不入Git和公开交付包。
回退先检查在途任务，恢复发布前Control镜像并按正式管理脚本发布、重新注册Worker和管理网络；Skill使用保留历史恢复。保持当前数据库及用户新增数据，不用旧SQLite直接覆盖。旧镜像没有本轮场景续接能力，回退不是功能等价。

## 限制
本次验证固定合成场景的续接、切换、中文约束与页面标记，不证明真实身份证查询或案件判断能力。模型并非绝对不会输出英文。当前事实Skill仍使用固定完成说明；此次验证不能替代对追问摘要细腻程度的专项评估。线上浏览器验证场景显示与三种宽度，清除后持久化由真实API验证，此前隔离浏览器已验证清除点击与刷新；不扩展为全部页面交互回归。
