# 场景Skill 2.2.0：可执行的后续建议

两个事实整理Skill取消2.1.0固定的一句成功/失败回复，改为回答用户问题、说明具体缺口、提供最多两项资料层面下一步选项。只引用approved支持的事实，部分成功不整批否定；查不到依据不等于没有记录。建议不自动执行，不扩大查询授权，不提供针对个人的犯罪判断或执法处置建议。

每个新的资料追问明确要求本轮prepare后check，失败不自动重试。该项是Skill生成约束，不是后端强制执行器；不能承诺模型绝不跳步。与代码自动保证工具顺序的后续改造分开。

发布范围：超级管理员的两个官方模板；alignment-a和alignment-b中与2.1.0源码完全一致的四个个人副本。名称、ID、启用状态、依赖不变，平台保留个人版本历史。其他用户或已编辑副本不覆盖。发布前内容备份到服务器受限skill-guidance-20260920目录，不纳入Git。

没有修改前端、模型自由文本历史或既有Run证据；旧截图不会自动改写，结构化步骤的状态标签继续如实显示。本轮不通过隐藏错误来伪装成功。

验证：检查两个文件的格式、流程及矛盾话术；平台PATCH受理；随后核对用户内容、配置applied/desired和技能加载接口。没有自动重发用户失败的问题；本轮未发起模型请求，因此回答措辞效果仍需后续真实验收。

示例仅用于说明预期结构，不是查询结果：这次还不能确认两人之间的记录，本轮缺少可核对的对应资料，这不代表没有记录。你可以选择重新整理当前场景，或查看上一轮来源详情。已确认的部分应先列出，不能用这个示例覆盖实际已成功结果。

回退：通过个人Skill上一版本恢复及备份模板内容执行；按平台配置应用流程生效，不手工改数据库，不覆盖历史会话。
