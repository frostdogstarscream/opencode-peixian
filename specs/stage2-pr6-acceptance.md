# PR-6 验收记录（候选）

基线 `679e4e6f7cbe60642c529e0ed840b172e7bd8370`。独立开发工作区，生产站点及前端未改动。

## 已执行

- 定向上下文、迁移及兼容回归：97 passed；随后增加六组可执行上下文语料及旧部署拒绝测试，纳入全量重跑。
- 首次 Control/Gateway 全量：844 passed / 1 failed。失败为 OpenAPI 旧字段白名单未包含 context_version，已同步契约断言，最终全量：846 passed，2 条现有依赖弃用警告。
- 部署、模板与当前交付工具：55 passed / 10 subtests passed。
- Python 3.12 隔离环境运行池交付与备份恢复：6 passed。
- 七独立插件 Bun：14 passed。
- Stage 1 recorded-evidence 校验通过；不代表重新验证镜像存在或生产运行。
- 历史 PR-5、PR-5.5、PR-5.6 在各自固定提交校验通过，交付工具测试分别 63、62、17 passed；旧文档未改写。

部署附加测试最初在主机 Python 3.11 上运行，出现 SQLite autocommit API 不支持；已改在规定的 Python 3.12 容器执行。新增测试模块加载错误也已修正，未将失败记录为通过。

## 证据边界

A 数据查询→B 解释→C 继续固定原始数据 Run；投影与 digest 入加密快照，模型自由文字不入事实。两个 Agent 均通过 HTTP MockTransport 的真实 Coordinator 投递协议测试，核对零插件及无需浏览器订阅的完成回报；这不是运行 Agent 容器或真实模型验收。

真实临时 SQLite 测试覆盖 v6→v7、事务中断回滚、重复打开、结构指纹拒绝、恢复升级前备份到空目录、跨账号、并发 CAS、重置后迟到完成和幂等重放。运行池备份恢复测试也已执行。未执行生产全套卷切换。

当前未部署、未更新 A/B、未访问真实数据、未请求模型、未进行负载测试；PR-7 尚未开始。GitHub CI 待候选提交后执行，不能用本地回归代替。
