# PR-8A 验收记录（候选）

25 项定向测试已通过，两个既有 Starlette/httpx 弃用警告。覆盖真实 Bun 插件、Gateway/Control 和本机 HTTP 合成服务；没有模型请求。包含 v8→v9 升级、重复启动、DDL 中断回滚、错误结构拒绝、空目录恢复、账号身份不变，及持久结果去重、防覆盖、密文内容摘要校验、账号/会话隔离、旧 Run 兼容、历史解释零新取数。

源码检查修正：迁移前驱引用、可信调用完成事件缺失、历史投影被后续源快照改变的防护。终态生成时间固定。测试夹具中的后续越权调用被拒绝，不被误写为全成功；恢复测试没有删权限检查来适配测试。

全量、部署、GitHub CI 与后续前端/线上验收尚在进行，未标记完成；结果详见本阶段 release.json。既有模型账本未消耗，本阶段模型请求0。

## 实现提交前验证

本地全量 927 passed / 1 failed，唯一失败是角色测试的 Health schema 枚举仍截止 v8。扩展为 v9 后角色相关回归通过。新结果/迁移及角色定向合计47项通过（新增编译器夹具修正后6项通过，其余41项此前通过），部署51项与10子测试通过，Bun14项通过。确切提交的全量通过声明等待GitHub CI，不将上述分批测试写成完整全量通过。

补充六类实际 Bun 编译器 + Control 持久执行记录用例覆盖两个Agent，资金整数单位、批准事实与计算事实不同来源、篡改Agent拒绝，v9结果备份恢复摘要一致。新增测试夹具最初误用导出名并缺少模块范围，已按实际 compile 接口修正，未改生产编译器。
