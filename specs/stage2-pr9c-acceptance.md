# PR-9C 验收进度

状态：候选开发完成，线上灰度尚未执行。

## 已完成

- PR-9B最终CI35676385717成功；保护规则23799147管理员读回通过。
- 同事新增right-rail.css与main.tsx导入已备份，独立提交ea3b4e215保留。
- 迁移及v7/v8/v9反例24 passed；灰度/制品/治理反例54 passed。
- 线上v6控制库只读副本迁移到v9：30张原业务表摘要未变化、101条Run保留、重复初始化不变；升级前后恢复到空目录通过。此项是控制库副本演练，不是完整停机备份或整个环境恢复。
- 实际旧v6镜像在另一v9副本上启动，被明确拒绝，不把只检查镜像标签视为实际拒绝证据。
- 灰度工具以固定完整RC1包运行验包/配置生成冒烟通过，未启动容器；最终部署需重新生成匹配RC2配置。长期运行配置不包含迁移批准开关。
- 四账号HTTPS登录/身份/退出通过：超管、管理员、普通A/B；凭据没有进入输出或Git。
- 前端Bun测试、类型检查、生产构建和隔离浏览器检查通过；不等同于线上页面或模型验证。

## 保留的启动错误

- 首次演练工具挂载到不符合仓库布局的短路径，模块初始化失败，尚未接触源库；按工具仓库结构重新挂载后成功。原日志保留。
- 首次前端误用不存在的npm test入口，未运行测试；随后采用CI中的bun test tests完成。未将入口失败记为产品测试失败，也未删除记录。

## 尚未完成

完整停写备份、全环境空目标恢复、第一阶段修复线上关账、A/B灰度、实际模型调用、Gateway重启/unknown、报告及页面线上验收。当前真实模型请求0/40，线上schema仍6，没有升级现有账号或替换运行镜像。
