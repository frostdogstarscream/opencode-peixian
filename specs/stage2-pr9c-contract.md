# PR-9C 小规模灰度与关账契约

基线为PR-9B关账71e7c7a15，最终CI35676385717全部成功。PR-9B在不可变工作树核验，后续源码不能使历史关账材料重新指向最新版本。

本轮先整合同事已备份的right-rail.css与导入，再构建新RC2，不改写RC1标签及其包。只使用现有alignment-a、alignment-b合成环境，保持账号、卷、会话、文件和旧Run归属。

顺序：副本6→9迁移及旧镜像拒绝 → 完整停写备份与空目标恢复 → 匹配Control/Gateway/Agent → 先A涉赌/盗窃与报告 → B重复 → 共性故障/隔离。线上第一阶段修复精确方法范围及Gateway启动身份核对需要另行记录，不能用旧静态检查代替。

配置由stage2-rollout.py根据完整验包清单生成，只允许legacy/a/ab三个范围。长期运行配置不包含迁移批准开关；离线迁移只在已备份且冻结时执行。工具不自行启动或停止服务。

stage2-migration-drill.py源库以SQLite只读连接取得一致快照，只修改新目录内副本；源是活动库副本时不声称完整停机备份。升级前后均恢复到空目录，比对原表内容摘要，重复初始化不改变数据。不清空未结束执行来强行通过迁移。

灰度前重新核对同事源文件及运行镜像；有新改动先保存整合。失败保留现场，v9不得由旧v6镜像接管；需完整恢复时使用空目标，不覆盖新增数据。

每轮用户级模型请求总上限40，先记账后发起；失败和未知计数、不自动重试。无模型测试、实际模型、页面和备份恢复分别记录。真实资料与大规模并发不在范围内。未通过或未执行任一必需项，PR-9C保持pending。
