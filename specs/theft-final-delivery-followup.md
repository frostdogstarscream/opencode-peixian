# M1—M4 候选封存补充记录

## 封存前修复

- 明确区分本次显式条件与继承的来源上下文。人员轨迹的历史时间不自动变成后续空间警情筛选；用户本次明确提出的条件仍不得丢弃。软剧本输入同时携带各能力字段合同及显式条件集合。
- 任务报告加入完整的选定来源索引。引用自任务外历史Run的条目也在同一只读快照内核对摘要、账号、记录和版本后导出，不能仅留下无内容的编号。原历史结果不改写。

定向回归21项通过。最终联合回归85 passed、2 warnings，覆盖本次新用例及M1—M4相关测试；耗时51.13秒。

## 镜像与依赖

标准Dockerfile构建因服务器访问Docker Hub超时未完成。已有缓存Control/Gateway底座的requirements.lock仅换行形式不同；按LF规范化后与源码完全一致，并逐项核对安装包版本无差异。使用独立/candidate代码路径构建验证镜像，不覆盖旧底座代码和线上容器。

前端已按源码声明版本重新安装独立依赖并保存package-lock，Vite7.1.4构建及typecheck通过，81项前端单测通过。根@tsconfig/bun解析警告和大包告警保留，尚无浏览器视觉和真实双账号交互验收。

## 完成声明

本轮为M1—M4开发候选，尚非最终交付验收。真实接口、模型规划行为、v11目标升级、非空双链路逐条复核及任务导出仍待联调。没有发送真实资料请求或真实模型请求，没有替换19460站点，没有推送GitHub。

目标迁移须在副本验证和停写前置满足后显式设置PX_ANALYSIS_TASKS_V1=1、PX_ALLOW_V11_MIGRATION=1；不能在运行中直接打开开关，不能让旧v10程序读取v11。此次未设置线上开关。
